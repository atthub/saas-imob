// ARQUIVO DESCONTINUADO — use prisma/seed.js (JavaScript puro).
// Mantido apenas para não quebrar referências antigas; o script real que
// roda em produção (inclusive automaticamente via server.js) é o seed.js.
export {};
